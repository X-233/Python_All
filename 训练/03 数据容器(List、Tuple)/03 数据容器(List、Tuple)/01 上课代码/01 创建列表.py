
# []  <中括号>代表列表
# <class 'list'>  list --> 列表类型,
# 后续咱们在对变量进行命名的时候不要用常用的函数或者类型 <int  float  bool> 的名字进行命名
# 列表这个数据容器, 内部可以存放任意的Python对象
list1 = [1, 2, 3.5, 'a', 'b', 'c', True, False, print]
print(list1)
print(type(list1))

print(str(list1))
print(type(str(list1)))

# 大家在后续查看数据的时候, 不要被数据表象的内容迷惑, 不要轻易的认定某个变量的类型
# Python是严格区分对象类型的, 而且不同的对象有不同的操作, 对象有对象的方法和属性
# 对象有对象的方法和属性

# 容器  --> 放东西的
# 增删改查
